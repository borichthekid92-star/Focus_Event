[authentication]

Ensure these match the values in your Flask app's config.ini

username = admin password = mikey455

[session]
secret_key = falcon_edr_secret_key_change_in_production_12345
session_timeout = 1800

[security]
max_login_attempts = 5
lockout_duration = 600

[analytics]

The main cycle runs every X hours. (Default: 12)

interval_hours = 12

The delay (in seconds) between generating the top/bottom list

and flagging the absolute least used one. (Default: 3600 seconds = 1 hour)

delay_seconds = 3600